#include "FileSystemInterface.hpp"
#include <string.h>

namespace Generix {

	/*fs_node::fs_node(CONST CHAR *name) : Type(FILE_TYPE_REGULAR), Permissions(ZERO), NoOfChilds(ZERO), Next(ZERO), Parent(ZERO), Child(ZERO) {
		memset(Name, ZERO, SIZEOF(Name));
		strcpy(Name, name);
	}*/


}
